ブラウザにデータを保存（ローカル ストレージ）

ローカル ストレージ スラッシュ セッション ストレージの違い
ローカル ストレージは「ブラウザ内に小さなデータを保存できる、手軽な保存場所」です（同一オリジン イコール 同じドメイン スラッシュ プロトコル スラッシュ ポートの範囲で共有されます）。

ローカル ストレージは ウィンドウ にぶら下がっているため、ページ内のジャバスクリプトから ローカル ストレージ と直接書けば呼び出せます（イコール ウィンドウ ドット ローカル ストレージ の省略形）。

基本的に「消すまで残る」永続領域です（ブラウザを閉じても残ります）。ただしユーザー操作やブラウザ設定（サイトデータ削除 スラッシュ ストレージ削除）で消えることはあります。

一方、セッション ストレージは「タブ スラッシュ ウィンドウ単位の一時置き場」です。同一オリジンでも別タブとは共有されず、タブを閉じると消えます。

どちらも文字列として保存されるため、機密情報（平文トークンなど）の保存は避けたい例です。使い分けは「永続で良いか」「そのタブを開いている間だけ使えれば良いか」で決めるとよいです。

保存・読み込み・削除の基本
エイピーアイはシンプルで、セット アイテム スラッシュ ゲット アイテム スラッシュ リムーブ アイテム（必要なら クリア）を使います。値は文字列として保存される点を押さえる必要があります。

コード例があります。

ユーアイでのテスト（ボタン操作など）を併用すると挙動把握が早いです。

ジェイソンで保存する定石
オブジェクトや配列は ジェイソン ドット ストリンギファイ で保存し、ジェイソン ドット パース で復元します。デート などの特殊型は アイエスオー 文字列にして保存し、読み込み時に ニュー デート で復元します。

要チェックとして、ストリンギファイ を忘れると「ブラケット オブジェクト オブジェクト ブラケット」など意図しない文字列が保存されます。
コード例があります。

保存データのバージョン管理（簡易）
将来的なフォーマット変更に備え、保存物にバージョンを持たせます。ブレース ブイ カンマ ペイロード ブレース 形式にすると読み込み時のマイグレーションが扱いやすいです。

読み込み時は ブイ を見て必要な変換を行い、互換性を保ちます。

壊れたデータへの復旧（トライ スラッシュ キャッチ）
外部編集や拡張機能で値が壊れていることがあるため、ジェイソン ドット パース は必ず トライ スラッシュ キャッチ で保護し、失敗したら該当キーを削除してデフォルトを返すのが安全です。

最小の ユーアイ とスクリプト（コードペン 等で動作確認可）です。

コード例があります。

保存 ヤジルシ 読み込み ヤジルシ 削除 の流れが正しく動くかを ユーアイ で確認します。
よくあるミス コンソールを開いていないため出力が見えない、または アイディー のスペル違い（セーブ ビーティーエヌ など）で ヌル になり アッド イベント リスナー で落ちることがあります。

使い分けの基準 永続なら ローカル ストレージ、タブを閉じたら消えてよいなら セッション ストレージ。保存形式は ブレース ブイ カンマ ペイロード ブレース にして将来の変更に備え、読み込みは トライ スラッシュ キャッチ プラス 失敗時削除で安全側に倒します。