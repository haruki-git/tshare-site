パイソンのインデントとコロン（:）の基本。

コロン（:）の役割（ブロック開始の合図）。

パイソンでは if スラッシュ for スラッシュ while スラッシュ def スラッシュ class スラッシュ try などの行末にコロン（:）を書きます。これは「ここから下にブロック（ひとかたまりの処理）が続く」という開始の合図です。

 コロン自体が処理を生むのではなく、「次の行をインデントしてブロックとして書く」ことまでがセットになります。

インデントの役割（パイソンは字下げでまとまりを表す）。

パイソンは {} の代わりにインデント（字下げ）でブロックを表します。そのため、インデントは見た目の整形ではなく、プログラムの構造そのものです。

 一般的にはペップ 8に従い「スペース4つ」で統一します。また、同じブロック内の行は必ず同じ幅で揃えます（混在するとエラーや意図しない挙動の原因になります）。

正しい書き方の例（if スラッシュ for スラッシュ def）。

最小コードです。

（コード例があります）

 貼り付け場所は sample.py に上から貼り付けて保存します。

 実行方法はターミナルで python sample.py を実行するか、ブイエスコードの実行ボタンを使います。

 確認方法です。

（コード例があります）

 上の assert が通ると「テスト成功」が表示されます。

よくあるエラー（SyntaxError: アンド #39 セミコロン : アンド #39 セミコロン 忘れ／IndentationError）。

よくあるエラー（SyntaxError: ':'忘れ／IndentationError） 
 コロンを忘れると、代表的には次のようなエラーになります（環境により表示文言は多少変わります）。

（コード例があります）

 
 
・ SyntaxError ： if x > 0: のように、条件や宣言の末尾に : が必要です。
 
・ IndentationError ：インデントが揃っていない、またはタブとスペースが混在していると発生します。
 

 よくあるミスは、タブとスペースの混在でインデントが崩れ、 IndentationError になることです（見た目が揃っているように見えても、内部的にはズレている場合があります）。